// Loop from the ASCII value of 'A' (65) to 'Z' (90)
for (var i = 65; i <= 90; i++) {
    // Convert the ASCII value to a character and print it
    var letter = String.fromCharCode(i);
    console.log(letter);
  }
  