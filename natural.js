function printNaturalNumbers(limit) {
    for (let i = 1; i <= limit; i++) {
      console.log(i);
    }
  }
  
  // Specify the limit to print natural numbers up to
  const limit = 19; // You can change this to any positive integer
  
  // Call the function to print natural numbers
  printNaturalNumbers(limit);
  